# Icons

Placeholder icons are included (a purple "A" mark) so the app loads and packages
cleanly. Replace with real AOWA branding before store release; keep the names
that `public/manifest.json` references:

- `icon256.png` — 256×256 color app icon
- `icon256_gray.png` — 256×256 grayscale (shown when the game isn't running)
- `icon.ico` — Windows launcher icon (multi-size)
