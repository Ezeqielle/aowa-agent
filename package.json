{
  "name": "aowa-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AOWA desktop companion (Overwolf) — syncs live Warframe inventory to AOWA.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "//": "For richer editor support, `npm i -D @overwolf/types` and add it to tsconfig `types`; it supersedes src/types/overwolf.d.ts.",
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
